[project]
name = "resumepilot-training"
version = "0.1.0"
description = "Reproducible offline model training for ResumePilot"
requires-python = ">=3.11,<3.13"
dependencies = [
  "safetensors>=0.5,<1",
  "torch>=2.6,<3",
  "transformers>=4.52,<5",
]

[tool.uv]
package = false
